services:
  - type: web
    name: binaire-freznel-assessment-server
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
